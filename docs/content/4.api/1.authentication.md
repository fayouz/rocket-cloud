---
title: Authentification
description: Les deux façons de s'authentifier auprès de l'API Rocket Cloud.
navigation:
  icon: i-lucide-lock
---

L'API est servie sous `/api`. Sa documentation OpenAPI interactive est disponible sur `/api/docs`. Les réponses sont en JSON (`Accept: application/json`) ou en JSON-LD (`application/ld+json`). Les `PATCH` attendent `Content-Type: application/merge-patch+json`.

## Utilisateur

```bash [Terminal]
curl -X POST https://cloud.exemple.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "alice@exemple.com", "password": "…"}'
# → { "token": "<jwt>" }
```

Le JWT est valable une heure et s'utilise avec `Authorization: Bearer <jwt>`. Les comptes LDAP se connectent avec leur mot de passe d'annuaire.

## Application

Un administrateur déclare l'application dans **Administration → Applications**. Son jeton secret (`rca_…`) n'est affiché qu'une fois ; seule son empreinte est stockée.

```http
Authorization: Bearer rca_…
```

Seule, une application ne peut qu'appeler `GET /api/me`. Pour agir, elle **impersonne** un utilisateur :

```http
Authorization: Bearer rca_…
X-Impersonate-User: alice@exemple.com
```

Elle obtient alors les droits de cet utilisateur, **sauf** le rôle administrateur. Cela suppose que l'application ait l'option « Peut agir en tant qu'utilisateur ». C'est ainsi qu'une application [dépose des fichiers](/api/files) dans l'espace de ses utilisateurs, ou [crée des liens de partage](/api/shares) en leur nom.

## Qui suis-je ?

```bash [Terminal]
curl https://cloud.exemple.com/api/me -H "Authorization: Bearer …" -H "Accept: application/json"
```

```json
{
  "user": { "id": "…", "email": "alice@exemple.com", "displayName": "Alice Durand" },
  "application": { "id": "…", "name": "CRM" },
  "roles": ["ROLE_USER", "ROLE_APPLICATION", "ROLE_IMPERSONATION"]
}
```

`roles` contient les rôles **effectifs** de la requête : une application qui impersonne un administrateur n'y voit pas `ROLE_ADMIN`.

## Pages publiques

Les seuls endpoints sans authentification propres à Rocket Cloud sont ceux des liens de partage, sous `/api/public/shares/{token}`. Voir [Partages](/api/shares#côté-public).
